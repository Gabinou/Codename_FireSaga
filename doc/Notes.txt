Make sure to have compatibility with FEBuilderGBA.

Tileset programs: Tilet/Mappy.
Tileset:


Turns out that OOP and c++ might be the way to go to: create object instance dynamically using factory pattern. 

NOTES on char array(C-strings) vs. std::strings. In general, C-strings are less convenient but lighter and faster than std::strings.
std:strings have more modern and intuitive interface, and more string manipulation capabilities. I think for FEmaker, C-strings are better. 
reasons: Memory and fastness is key. Also names can be changes using 2 lines of code:      char temp[14] = "Marth1"; strncpy(Marth.name, temp, sizeof(temp));

For some reason, in C the struct is ligther than the multidimensional array. Also enables multiple datatypoes. Input is a little bit less clear, but with a little bit of formatting it can look okay.

Idea to adde mechanics: layer of Air tiles on top of the ground tiles that add status effects like fog of war, poison, damage over time, wind to limit flyer movement, weather, etc.
Design notes: add a 3rd dimension for flying and making some mountains impassable even for flying units except high altitude flyers. Maybe for promoted flyers? Maybe have special status effects?

Next step: make the files that make weapons, and characters into one separate header or include.

Other weapons:
    new type: sticks with hidden weapon the cross? lol maybe stick is the most basic lance?
    Other materials for weapons: mythril, platinum, adamantine, magnetite, sun sword, moon sword.


BOWS IDEAS:
    Make bows and arrows possible, with combinations of the two. Maybe make it possible for archers to hit back with the bow (for minimal damage) or use the bow to block incoming attacks? or reduce incoming damage?