#include "command.hpp"




void InputHandler::handleInput() {
    // if (isPressed(BUTTON_UP)) buttonX_->execute();
    // else if (isPressed(BUTTON_Y)) buttonY_->execute();
    // else if (isPressed(BUTTON_A)) buttonA_->execute();
    // else if (isPressed(BUTTON_B)) buttonB_->execute();
}