#include "minctest.h"
#include "platform.h"
#include "enums.h"

void test_enums() {

    // lok(ITEM_TYPE_EXP_SWORD == 0);

    // lok(UNIT_TYPE_EXP_HUMAN == 0);
    // lok(UNIT_NAME_PC_EXP_ERWIN == 0);
    // lok(UNIT_NAME_NPC_EXP_ZIDINE == 0);
}